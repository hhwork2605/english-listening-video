# Sao chép file này thành .env rồi điền khoá. .env đã được .gitignore.
# KHÔNG để khoá thật trong file .env.example này (nó bị git theo dõi).

# ElevenLabs (giọng tự nhiên cho hội thoại) — dùng với:
#   npm run dialogue:audio:eleven -- --data projects/<id>/dialogue.json
ELEVENLABS_API_KEY=
# (tuỳ chọn) ghi đè giọng mặc định 2 nhân vật bằng voice ID ElevenLabs:
# ELEVEN_VOICE_A=21m00Tcm4TlvDq8ikWAM   # nữ (A)
# ELEVEN_VOICE_B=pNInz6obpgDQGcFmaJgB   # nam (B)
# (tuỳ chọn) model / định dạng. Mặc định eleven_v3 (hỗ trợ audio tag cảm xúc
# [laughs] [whispers] [excited]...). Dùng eleven_multilingual_v2 nếu cần ổn định.
# ELEVENLABS_MODEL=eleven_v3
# ELEVENLABS_FORMAT=mp3_44100_128

# ElevenLabs web — LÁI WEB elevenlabs.io bằng Playwright, dùng CREDIT tài khoản web
# thay vì API key. Dùng với:
#   npm run dialogue:audio:eleven:web -- --data projects/<id>/dialogue.json --cdp 9223
# Đăng nhập: mở Chrome với --remote-debugging-port=9223 --user-data-dir=%USERPROFILE%\eleven-chrome,
# đăng nhập elevenlabs.io 1 lần, rồi chạy script. Giọng dùng TÊN HIỂN THỊ trên web
# (My Voices), KHÔNG phải voice ID. Model chọn tay 1 lần trên web (web tự nhớ).
# ELEVEN_WEB_VOICE_A=Rachel
# ELEVEN_WEB_VOICE_B=Adam
# ELEVEN_CDP=9223
# ELEVEN_PACE_MS=2500
# ELEVEN_AUDIO_WAIT_MS=90000

# aivideoauto Voice Studio — LÁI WEB aivideoauto.com/audio bằng Playwright
# (API gommo cũ đã ngừng). Dùng với:
#   npm run dialogue:audio:aiva -- --data projects/<id>/dialogue.json
#   (rồi chạy dialogue:align để có words[] cho karaoke)
# Đăng nhập: lần đầu chạy headed sẽ mở cửa sổ để đăng nhập tay (lưu vào .pw-profile/),
# hoặc gắn vào Chrome thật đã đăng nhập:  --cdp 9222  /  AIVA_CDP=9222
# AIVA_PROVIDER=ElevenLabs        # Omnivoice | ElevenLabs | MiniMax
# AIVA_MODEL=Eleven V3            # đúng tên trong dropdown model (xem đầu scripts/tts-aivideoauto.mjs)
# AIVA_VOICE_A=                   # từ khóa/tên/ID giọng trong "Thư viện giọng" cho speaker A
# AIVA_VOICE_B=                   # giọng PHẢI khớp provider (lọc "Nguồn" trong thư viện)
# AIVA_CDP=
# AIVA_GEN_TIMEOUT_MS=240000

# OpenAI TTS (định dạng câu đơn script.json, adapter generate-audio.ts)
# OPENAI_API_KEY=
# TTS_PROVIDER=openai

# GenMax (https://genmax.io — gateway TTS: ElevenLabs / MiniMax / CapCut, adapter tts-genmax.mjs)
# GENMAX_API_KEY=
# GENMAX_PROVIDER=elevenlabs
# GENMAX_VOICE_A=21m00Tcm4TlvDq8ikWAM
# GENMAX_VOICE_B=pNInz6obpgDQGcFmaJgB
