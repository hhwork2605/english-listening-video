node_modules/
out/
.env
.DS_Store
projects/
# Audio sinh ra theo project (buffer) — giữ .gitkeep, bỏ qua phần còn lại
public/audio/*
!public/audio/.gitkeep
# Profile trình duyệt Playwright/Chrome (chứa cookie/phiên đăng nhập — KHÔNG commit)
.pw-profile*/
__pycache__/
# Webhook Sheet (chứa token) — không commit
ledger/webhook.json
ledger/.post.json
public/backgrounds/*
